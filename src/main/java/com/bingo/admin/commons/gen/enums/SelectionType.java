package com.bingo.admin.commons.gen.enums;

public enum SelectionType {
	/*
	 * 数组形式（index为值，value为显示字段）
	 */
	// ArrayList,
	/*
	 * map形式（key为值，value为显示字段）
	 */
	// Map,
	/*
	 * 枚举形式（oragin为key,可指定vlaue属性）
	 */
	Enum,
	/*
	 * 对象数组形式（可指定对象属性为key和vlaue）
	 */
	ObjectList
}
